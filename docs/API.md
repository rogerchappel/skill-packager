# API

- `scaffoldSkill(dir, name)`: create a minimal reusable skill package.
- `validateSkill(dir)`: check required files and approval language.
- `requiredFiles`: canonical file list for v1 packages.
